## Removes metadata tags from MP4 files

Invoke with python and no arguments to scrub metadata in the CWD or add the target directory.

Example:

python scubber.py
python scubber.py /home/user/videos
